# prometheus-demo
# Steps to build API
  - `npm install`
  - `nodemone index.js`
  - Server is runing on port 4000
  
# Steps to build UI
  - `npm install`
  - `npm start`
  - Rum applicaiton on port 3000 `http://localhost:3000/`
